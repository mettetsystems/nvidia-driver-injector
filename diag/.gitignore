# diag/ build + transient artefacts.
# Baseline reading files (baseline-*.txt) ARE committed — they are the
# known-good reference set, not build scratch.

# CMake / build scratch (shouldn't appear here — builds happen in
# the Docker stage — but ignore defensively in case someone runs
# cmake locally inside diag/).
build/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# Local docker compose state
.env

# Python
__pycache__/
*.pyc
